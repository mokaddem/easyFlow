{
    "multiplex_logic": {
        "label": "Multiplexer logic",
        "DOM": "select",
        "options": ["Interleave", "Priority"],
        "default": "Interleave",
        "required": "true"
    }
}
