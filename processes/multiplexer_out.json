{
    "multiplex_logic": {
        "label": "Multiplexer logic",
        "DOM": "select",
        "options": ["Interleave", "Priority", "Duplicate"],
        "default": "Interleave",
        "required": true,
        "dynamic_change": true
    }
}
